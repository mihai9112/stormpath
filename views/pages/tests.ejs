<html lang="en">
<head>
<style>
    table, th, td {
        border: 1px solid black;
        border-collapse: collapse;
    }
    th, td {
        padding: 5px;
        text-align: left;
    }
</style>
</head>
<body>
    <table>
        <tr>
            <th>Call index</th>
            <th>Query total</th>
            <th>Tractor total</th>
            <th>Contention</th>
            <th>Total</th>
        </tr>
        <% renderThisResults.forEach(function(result) { %>
            <tr>
                <td><%= result.index %></td>
                <td><%= result.qTotal %>ms</td>
                <td><%= result.tTotal %>ms</td>
                <td><%= result.total - (result.qTotal + result.tTotal) %>ms</td>
                <td><%= result.total %>ms</td>
            </tr>
        <% }); %>
    </table>
</body>
</html>